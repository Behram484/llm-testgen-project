package freemind.controller.filter.util;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

@Test
void testAddAndGetValueByIndex() {
 Integer key = 5;
 Object value = "test"; // Updated from String to Object
 
 int index = map.add(key, value);
 assertEquals(value, map.getValue(index));
}