Fix the compile errors in the JUnit 5 test class below.

Output rules:
- Output ONLY Java code
- Do NOT use markdown
- The first line must be: package {PACKAGE};
- The class name must remain: {TEST_CLASS_NAME}

Strict requirements:
- Only goal: make the test class compile
- Do NOT add new tests or improve coverage
- Do NOT change test logic unless it causes a compile error
- Fix imports, method calls, signatures, constructors, and type errors
- Use only methods that exist in the CUT below
- Use assertEquals and assertThrows only
- Do NOT invent methods, fields, constructors, classes, or helper functions
- Prefer deleting a broken test over inventing code to fix it

Patch-style compile fix constraints:
- Apply minimal edits only (imports, signatures, argument types, constructors, symbols)
- Do NOT rewrite unaffected methods
- Use only these CUT public API signatures:
{METHOD_SIGNATURES}

Java class under test:
<CODE>

{DEPENDENCY}

Current test class (has compile errors):
{FAILED_METHODS}

Compile errors:
{FAILURES}

{SUCCESSFUL_REFERENCE}

Output the full corrected JUnit 5 test class only.
